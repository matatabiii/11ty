{
  "name": "11ty",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "watch:sass": "sass --no-source-map --watch src/assets/sass:dist/assets/css",
    "watch:eleventy": "eleventy --serve",
    "build:sass": "sass --no-source-map src/assets/sass:dist/assets/css",
    "build:eleventy": "eleventy",
    "clean": "del dist",
    "start": "npm-run-all clean build:sass --parallel watch:*",
    "build": "npm-run-all clean build:sass build:eleventy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "autoprefixer": "^10.2.6",
    "del-cli": "^4.0.0",
    "npm-run-all": "^4.1.5",
    "sass": "^1.35.1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "sass-easing": "^1.0.3"
  }
}
